# Helper Script: test-early-return
# Tests explicit return statement

sysmsg "  -> Early return script started" 58
overhead "Testing early return..."
wait 300

if hp > 0
    sysmsg "  -> Returning early (hp > 0)" 58
    overhead "Early return condition met"
    return
endif

# This should never execute
sysmsg "  -> ERROR: This should not print!" 38
overhead "ERROR: After return executed!"
